version: "3.8"
services:
  adguard:
    container_name: ${SERVICE}
    image: 
    restart: unless-stopped
    ports:
      -
    volumes:
      -
    networks:
      - 
    labels:
      - 
    environment:
      -
    # depending if required
    network_mode: 
    user:
    command:
      -
    healthcheck:
      test: ["executable", "arg"]
      interval: 1m30s
      timeout: 30s
      retries: 5
      start_period: 30s
    depends_on:
      - serviceName
    ulimits:

volumes:

networks:
