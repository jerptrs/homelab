[defaults]
inventory = inventory.yml
private_key_file = ~/.ssh/ansible
remote_user = ansible
host_key_checking = False
retryfilesenabled = False

[privilege_escalation]
become=true
become_method=su
become_user=ansible