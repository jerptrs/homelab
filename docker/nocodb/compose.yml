services:
  nocodb:
    image: nocodb/nocodb:latest
    container_name: nocodb
    ports:
      - "8081:8080"
    volumes:
      - nocodb:/usr/app/data/
    restart: unless-stopped

volumes:
  nocodb: